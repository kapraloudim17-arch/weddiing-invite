<!DOCTYPE html>
<html lang="el">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Ιωάννης & Μαρία</title>

  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:wght@400;500;600;700&family=Great+Vibes&display=swap" rel="stylesheet">

  <style>
    :root{
      --bg1:#eef4fb;
      --bg2:#f8fbff;
      --blue1:#d8e4f5;
      --blue2:#b8cae5;
      --blue3:#92add6;
      --paper:#fffdf9;
      --ink:#2d3450;
      --shadow:0 18px 45px rgba(49,67,104,.16);
      --soft:0 10px 24px rgba(49,67,104,.14);
    }

    *{ box-sizing:border-box; -webkit-tap-highlight-color:transparent; }

    body{
      margin:0;
      min-height:100vh;
      overflow:hidden;
      display:flex;
      align-items:center;
      justify-content:center;
      background:
        radial-gradient(circle at top, #ffffff 0%, var(--bg2) 35%, var(--bg1) 100%);
      font-family:'Cormorant Garamond', serif;
      color:var(--ink);
    }

    .scene{
      position:relative;
      width:min(94vw, 430px);
      height:min(90vh, 760px);
      display:flex;
      align-items:center;
      justify-content:center;
    }

    /* στοιχεία που εμφανίζονται αφού πατηθεί ο φάκελος */
    .reveal{
      opacity:0;
      transform:translateY(20px) scale(.97);
      transition:all .8s ease;
      pointer-events:none;
    }

    .scene.opened .reveal{
      opacity:1;
      transform:translateY(0) scale(1);
      pointer-events:auto;
    }

    .halo{
      position:absolute;
      width:78%;
      height:78%;
      border-radius:50%;
      background:radial-gradient(circle, rgba(255,255,255,.9) 0%, rgba(255,255,255,.22) 45%, transparent 70%);
      filter:blur(8px);
      z-index:0;
    }

    /* polaroids */
    .polaroid{
      position:absolute;
      width:116px;
      background:#fff;
      padding:8px 8px 18px;
      box-shadow:var(--soft);
      border-radius:2px;
      z-index:1;
    }

    .polaroid img{
      width:100%;
      height:94px;
      object-fit:cover;
      display:block;
      background:#e8edf7;
    }

    .photo-left{
      left:26px;
      top:110px;
      transform:rotate(-8deg);
    }

    .photo-right{
      right:26px;
      top:255px;
      transform:rotate(8deg);
    }

    /* flowers */
    .flowers{
      position:absolute;
      z-index:1;
      width:90px;
      height:140px;
    }

    .flowers.left{
      left:8px;
      bottom:165px;
      transform:rotate(-8deg);
    }

    .flowers.right{
      right:8px;
      bottom:90px;
      transform:rotate(10deg);
    }

    .flower-head{
      position:absolute;
      width:68px;
      height:68px;
      left:10px;
      top:0;
      border-radius:50%;
      background:
        radial-gradient(circle at 35% 35%, #eef4ff 0 18%, #c0d0ed 19% 42%, #94add9 43% 61%, transparent 62%);
      filter:drop-shadow(0 8px 10px rgba(90,110,150,.14));
    }

    .stem{
      position:absolute;
      width:6px;
      height:85px;
      left:40px;
      top:54px;
      border-radius:10px;
      background:linear-gradient(to bottom, #d1e1b9, #95ad7f);
    }

    .leaf{
      position:absolute;
      width:18px;
      height:34px;
      border-radius:50%;
      background:linear-gradient(to bottom, #dbe8c9, #9cb683);
    }

    .leaf.l1{ left:18px; top:70px; transform:rotate(-35deg); }
    .leaf.l2{ left:45px; top:92px; transform:rotate(28deg); }
    .leaf.l3{ left:14px; top:108px; transform:rotate(-42deg); }

    /* vinyl */
    .record-wrap{
      position:absolute;
      right:36px;
      top:120px;
      width:112px;
      height:112px;
      z-index:2;
    }

    .record{
      width:100%;
      height:100%;
      border:none;
      border-radius:50%;
      cursor:pointer;
      position:relative;
      background:
        radial-gradient(circle at center, #7a7a7a 0 10%, #171717 11% 22%, #000 23% 100%);
      box-shadow:var(--soft);
    }

    .record::before{
      content:"";
      position:absolute;
      inset:10px;
      border-radius:50%;
      background:repeating-radial-gradient(circle, rgba(255,255,255,.05) 0 1px, transparent 1px 6px);
    }

    .record::after{
      content:"";
      position:absolute;
      left:50%;
      top:50%;
      width:14px;
      height:14px;
      transform:translate(-50%,-50%);
      border-radius:50%;
      background:#ddd;
      box-shadow:0 0 0 5px #7e7e7e, 0 0 0 9px #111;
    }

    .record.playing{
      animation:spin 3s linear infinite;
    }

    .record-label{
      text-align:center;
      margin-top:8px;
      font-size:15px;
      letter-spacing:.5px;
    }

    @keyframes spin{
      from{ transform:rotate(0deg); }
      to{ transform:rotate(360deg); }
    }

    /* envelope area */
    .envelope-area{
      position:relative;
      width:320px;
      height:420px;
      z-index:3;
    }

    .card{
      position:absolute;
      left:50%;
      bottom:118px;
      width:232px;
      height:305px;
      transform:translateX(-50%) translateY(170px);
      background:linear-gradient(180deg, #fff 0%, var(--paper) 100%);
      border-radius:6px;
      padding:18px 18px 22px;
      box-shadow:var(--shadow);
      z-index:2;
      transition:transform 1s cubic-bezier(.2,.8,.2,1), box-shadow .35s ease;
      cursor:pointer;
      overflow:hidden;
      pointer-events:none;
    }

    .scene.opened .card{
      transform:translateX(-50%) translateY(38px);
      pointer-events:auto;
    }

    .scene.card-out .card{
      transform:translateX(-50%) translateY(-86px);
      box-shadow:0 24px 60px rgba(49,67,104,.24);
    }

    .card::before{
      content:"";
      position:absolute;
      inset:10px;
      border:1px solid rgba(120,130,160,.22);
    }

    .monogram{
      width:50px;
      height:50px;
      margin:4px auto 8px;
      border:1px solid rgba(110,120,150,.25);
      border-radius:50%;
      display:flex;
      align-items:center;
      justify-content:center;
      font-size:22px;
      font-weight:600;
    }

    .small-title{
      text-align:center;
      font-size:15px;
      letter-spacing:2px;
      text-transform:uppercase;
      margin-top:2px;
    }

    .date{
      text-align:center;
      font-size:36px;
      line-height:1;
      margin-top:8px;
      letter-spacing:2px;
    }

    .mini{
      text-align:center;
      font-size:12px;
      letter-spacing:1.5px;
      text-transform:uppercase;
      margin-top:10px;
      opacity:.8;
    }

    .names{
      text-align:center;
      margin-top:8px;
      line-height:1;
    }

    .script{
      font-family:'Great Vibes', cursive;
      font-size:35px;
    }

    .amp{
      display:block;
      font-size:26px;
      margin:5px 0;
    }

    .details{
      margin-top:16px;
      text-align:center;
      font-size:15px;
      line-height:1.35;
      opacity:0;
      transform:translateY(16px);
      transition:all .6s ease;
      padding:0 8px;
    }

    .scene.card-out .details{
      opacity:1;
      transform:translateY(0);
      transition-delay:.2s;
    }

    .envelope-back{
      position:absolute;
      left:50%;
      bottom:58px;
      transform:translateX(-50%);
      width:282px;
      height:186px;
      background:linear-gradient(180deg, var(--blue1), var(--blue2));
      border-radius:10px;
      box-shadow:var(--shadow);
      overflow:hidden;
      z-index:1;
    }

    .envelope-back::before{
      content:"";
      position:absolute;
      left:0;
      bottom:0;
      width:50%;
      height:100%;
      background:rgba(255,255,255,.12);
      clip-path:polygon(0 0, 100% 100%, 0 100%);
    }

    .envelope-back::after{
      content:"";
      position:absolute;
      right:0;
      bottom:0;
      width:50%;
      height:100%;
      background:rgba(255,255,255,.07);
      clip-path:polygon(100% 0, 100% 100%, 0 100%);
    }

    .flap-top{
      position:absolute;
      left:50%;
      bottom:150px;
      transform:translateX(-50%) rotateX(0deg);
      transform-origin:top center;
      width:282px;
      height:118px;
      transition:transform 1s cubic-bezier(.2,.8,.2,1);
      z-index:5;
      filter:drop-shadow(0 10px 14px rgba(49,67,104,.12));
    }

    .flap-top::before{
      content:"";
      position:absolute;
      inset:0;
      background:linear-gradient(180deg, #b6c8e5, #97afd7);
      clip-path:polygon(0 0, 100% 0, 50% 100%);
      border-radius:4px;
    }

    .scene.opened .flap-top{
      transform:translateX(-50%) rotateX(180deg);
      z-index:0;
    }

    .front-pocket{
      position:absolute;
      left:50%;
      bottom:58px;
      transform:translateX(-50%);
      width:282px;
      height:160px;
      z-index:6;
      cursor:pointer;
    }

    .front-pocket::before{
      content:"";
      position:absolute;
      inset:0;
      background:linear-gradient(180deg, #aabde0, #8ea8d3);
      clip-path:polygon(0 100%, 0 36%, 50% 78%, 100% 36%, 100% 100%);
      border-radius:0 0 10px 10px;
      box-shadow:0 10px 22px rgba(49,67,104,.12);
    }

    .seal{
      position:absolute;
      left:50%;
      bottom:18px;
      transform:translateX(-50%);
      width:66px;
      height:66px;
      border-radius:50%;
      background:radial-gradient(circle at 30% 30%, #fbf4f7 0%, #eee0e7 60%, #d8c7cf 100%);
      z-index:7;
      display:flex;
      align-items:center;
      justify-content:center;
      font-family:'Great Vibes', cursive;
      font-size:24px;
      color:#fff;
      text-shadow:0 1px 2px rgba(0,0,0,.08);
      box-shadow:0 5px 12px rgba(60,60,80,.15);
      pointer-events:none;
    }

    .initial-view{
      transition:all .8s ease;
    }

    .scene.opened .initial-view{
      transform:translateY(20px);
    }

    .hint{
      position:absolute;
      left:50%;
      bottom:18px;
      transform:translateX(-50%);
      background:rgba(34,44,66,.82);
      color:#fff;
      padding:10px 16px;
      border-radius:999px;
      font-size:15px;
      z-index:10;
      transition:all .35s ease;
      max-width:90%;
      text-align:center;
    }

    .hint.hide{
      opacity:0;
      transform:translateX(-50%) translateY(10px);
      pointer-events:none;
    }

    @media (max-width:460px){
      .photo-left{ left:20px; top:120px; }
      .photo-right{ right:18px; top:270px; }
      .record-wrap{ right:24px; top:126px; width:104px; height:104px; }
    }
  </style>
</head>
<body>
  <div class="scene" id="scene">
    <div class="halo reveal"></div>

    <div class="polaroid photo-left reveal">
      <img src="photo1.jpg" alt="photo 1">
    </div>

    <div class="record-wrap reveal">
      <button class="record" id="record" aria-label="Play music"></button>
      <div class="record-label">Πάτησε το βινύλιο</div>
    </div>

    <div class="polaroid photo-right reveal">
      <img src="photo2.jpg" alt="photo 2">
    </div>

    <div class="flowers left reveal">
      <div class="flower-head"></div>
      <div class="stem"></div>
      <div class="leaf l1"></div>
      <div class="leaf l2"></div>
      <div class="leaf l3"></div>
    </div>

    <div class="flowers right reveal">
      <div class="flower-head"></div>
      <div class="stem"></div>
      <div class="leaf l1"></div>
      <div class="leaf l2"></div>
      <div class="leaf l3"></div>
    </div>

    <div class="envelope-area initial-view">
      <div class="card" id="card">
        <div class="monogram">ΙΜ</div>
        <div class="small-title">Προσκλητήριο Γάμου</div>
        <div class="date">25.07.2026</div>
        <div class="mini">Στον γάμο των</div>

        <div class="names">
          <div class="script">Ιωάννης</div>
          <span class="amp">&</span>
          <div class="script">Μαρία</div>
        </div>

        <div class="details">
          Με χαρά σας προσκαλούμε να τιμήσετε με την παρουσία σας τον γάμο μας.<br><br>

          <strong>Γονείς</strong><br>
          Σωτήρης & Αγάπη Κυριακοπούλου<br>
          Γεώργιος & Ελένη Πατζού<br><br>

          <strong>Ώρα</strong> 7:00 μ.μ.<br>
          <strong>Ημερομηνία</strong> 25/7/2026<br>
          <strong>Τοποθεσία</strong> Κτήμα Ίριδα, Μαρκόπουλο
        </div>
      </div>

      <div class="flap-top"></div>
      <div class="envelope-back"></div>
      <div class="front-pocket" id="envelopeTap"></div>
      <div class="seal">ΙΜ</div>
    </div>

    <div class="hint" id="hint">Πάτησε τον κλειστό φάκελο</div>

    <audio id="music" src="song.mp3"></audio>
  </div>

  <script>
    const scene = document.getElementById("scene");
    const envelopeTap = document.getElementById("envelopeTap");
    const card = document.getElementById("card");
    const hint = document.getElementById("hint");
    const record = document.getElementById("record");
    const music = document.getElementById("music");

    let envelopeOpened = false;
    let cardPulled = false;

    envelopeTap.addEventListener("click", () => {
      if (!envelopeOpened) {
        envelopeOpened = true;
        scene.classList.add("opened");
        hint.textContent = "Τώρα πάτησε πάνω στο προσκλητήριο";
      }
    });

    card.addEventListener("click", (e) => {
      e.stopPropagation();
      if (!envelopeOpened) return;

      if (!cardPulled) {
        cardPulled = true;
        scene.classList.add("card-out");
        hint.classList.add("hide");
      }
    });

    record.addEventListener("click", (e) => {
      e.stopPropagation();

      if (music.paused) {
        music.play();
        record.classList.add("playing");
      } else {
        music.pause();
        record.classList.remove("playing");
      }
    });

    music.addEventListener("ended", () => {
      record.classList.remove("playing");
    });
  </script>
</body>
</html>